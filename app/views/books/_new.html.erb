<h2>New book</h2>

    <%= form_with model: book do |f| %>
      <h4>Title</h4>
        <%= f.text_field :title %>
      <h4>Opinion</h4>
        <%= f.text_area :body %>
      <nav>
        <%= f.submit 'Create Book', class: "col-xs-3 btn-sm btn-success"%>
      </nav>
    <% end %>
